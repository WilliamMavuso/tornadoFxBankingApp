interface Transactions {

    fun deposit(amount:Int) {
        var amount = amount
    }

    fun withdraw(amount:Int) {
        var amount = amount
    }

   fun calculateTransactionFee(fee:Int){
       var deposit = true
       var withdrawal = false

       if (deposit != true){
           fee
       }
    }

//    fun getInterest
}